# Results

This directory is reserved for lightweight result summaries and small generated artifacts.

Large generated files are ignored by default:

- trained models,
- TensorFlow Lite files,
- raw datasets,
- output CSV/PNG artifacts.

Keep reproducible commands and metrics in run-specific output directories under `outputs/`, or summarize stable findings here when they are small enough to track.
